<?xml version="1.0"?>
<project name="env.build" default="concat" basedir=".">
    <description>Build file for module</description>

    <import file="../../../build.xml"/>

	<property name="module.dir" value="."/>
    <property name="module.name" value="validate"/>

    <target name="concat">
		<mkdir dir="${build.dir}/order/${module.name}"/>
        <concat destfile="${build.dir}/te/${module.name}/index.css" encoding="${charset}" outputencoding="${charset}">
            <fileset dir="${module.dir}" includes="*.css"/>
        </concat>
        <concat destfile="${build.dir}/te/${module.name}/index.js" encoding="${charset}" outputencoding="${charset}">
            <fileset dir="${module.dir}" includes="*.js"/>
        </concat>
    </target>



</project>
